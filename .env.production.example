# Copy this file to .env.production on the deployment host.
# Keep the loopback URL until a formal domain and HTTPS endpoint are approved.
NEXT_PUBLIC_SITE_URL=http://127.0.0.1:3000

# CI replaces this with an immutable GHCR image tag only after deployment is enabled.
APP_IMAGE=fazhi-shengjing:latest
